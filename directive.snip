snippet ngdirective
options head
  (function() {
    'use strict';
    
    angular
      .module('${1:#:module}')
      .directive('${2:#:directive}', $2);
    
    /* @ngInject */
    function $2(${3:#:dependencies}) {
      var directive = {
            link: link,
            restrict: 'A'
          };
      return directive;
      
      function link(scope, element, attrs) {
      }
    }
  })();
